{
  "name": "simple-booking-api-spec",
  "version": "1.0.0",
  "private": true,
  "description": "Контракт «Запись на звонок» — TypeSpec, источник правды для фронтенда и бэкенда",
  "scripts": {
    "build": "tsp compile ."
  },
  "devDependencies": {
    "@typespec/compiler": "^1.5.0",
    "@typespec/http": "^1.5.0",
    "@typespec/openapi": "^1.5.0",
    "@typespec/openapi3": "^1.5.0"
  }
}
